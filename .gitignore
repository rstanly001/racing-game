__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/
.DS_Store
.idea/
.vscode/

# Personal planning notes, not part of the package.
COMMIT_PLAN.md

# Local dependency resolution. The package declares what it needs in
# pyproject.toml, which is what an installer reads.
uv.lock

# The generated figures and telemetry in output/ are committed on purpose,
# so they are deliberately not ignored here.
